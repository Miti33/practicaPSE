package com.mycompany.practicafinalpse.refugio;

import java.util.List;

public class RestClient {
    /*public static List<SolicitudDTO> obtenerSolicitudesPorRefugioYEstado(String estado) {
        // Aquí va una llamada GET a tu servicio REST, ejemplo con JAX-RS Client:
        // Realizas la llamada y parseas los datos
        return <0>;
    }
*/

    public static void actualizarEstadoSolicitud(int id, String nuevoEstado) {
        // Llamada PUT o POST a tu REST para cambiar el estado
        // Enviar JSON con {"estado": nuevoEstado}
    }
}
